# Document tooling

Reproducible PRD export and verification scripts will live here after the canonical PRD is authored. Tooling may generate governed documents but must not contain application runtime behavior.
