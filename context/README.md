# Six-file project context

This directory is reserved for the concise agent context system: project overview, architecture, code standards, AI workflow rules, UI context, and progress tracker. Those files will be authored from the approved PRD rather than guessed during scaffolding.
